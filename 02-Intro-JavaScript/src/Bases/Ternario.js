const activo = true

const mensaje = (activo) ? 'Activo': 'Inactivo'
//Si la primer condicion se cumple, retorna lo que esta despues del and
const otro = activo && 'Activo'

console.log(mensaje)
console.log(otro)
